keytool -genkey -alias meryem -keyalg RSA -keystore meryem.jks -keysize 2048
keytool -export -alias meryem -keystore meryem.jks -rfc -file publickey.cert

****************************************************************************

C:\Docs2021\apps\crypto>keytool -genkey -alias meryem -keyalg RSA -keystore meryem.jks -keysize 2048
 for: CN=meryem ouyouss, OU=ensa, O=ensa, L=rabat, ST=rabat, C=ma

  keytool -export -alias meryem -keystore meryem.jks -rfc -file publicKey.cert

 Enter keystore password:
 Certificate stored in file <publicKey.cert>

Warning:
Le fichier de clés JKS utilise un format propriétaire. Il est recommandé de migrer vers PKCS12, qui est un format standard de l'industr
ie en utilisant "keytool -importkeystore -srckeystore meryem.jks -destkeystore meryem.jks -deststoretype pkcs12".

C:\Docs2021\apps\crypto>keytool -export -alias meryem -keystore meryem.jks -rfc -file publickey.cert
Entrez le mot de passe du fichier de clés :
Certificat stocké dans le fichier <publickey.cert>

Warning:
Le fichier de clés JKS utilise un format propriétaire. Il est recommandé de migrer vers PKCS12, qui est un format standard de l'industr
ie en utilisant "keytool -importkeystore -srckeystore meryem.jks -destkeystore meryem.jks -deststoretype pkcs12".